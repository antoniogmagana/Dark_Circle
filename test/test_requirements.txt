# Test Requirements for Dark Circle Test Suite
# Install with: pip install -r test_requirements.txt

# Testing framework
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.5.0  # For parallel test execution

# Core dependencies (matching main requirements where applicable)
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# Database testing
psycopg2-binary>=2.9.0

# Additional testing utilities
mock>=5.1.0
hypothesis>=6.92.0  # Property-based testing

# Coverage and reporting
coverage>=7.3.0

# Optional: for better test output
pytest-html>=4.1.0
pytest-json-report>=1.5.0

# Audio/signal processing (if needed for integration tests)
torchaudio>=2.0.0
scipy>=1.10.0

# Machine learning frameworks
scikit-learn>=1.3.0

# Time series models (for MINIROCKET tests if needed)
tsai>=0.3.0  # Optional, only if testing MINIROCKET models
