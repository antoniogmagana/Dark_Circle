# Build staging area for docker contexts that need files from
# outside the inference-engine directory (e.g., model checkpoints
# from model-train/).
build/

# Python / venv junk
.venv/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store
