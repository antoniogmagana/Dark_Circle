apiVersion: apps/v1
kind: Deployment
metadata:
  name: infer-classify
  labels:
    app: infer-classify
spec:
  replicas: {{ .Values.inference.inferClassify.replicas }}
  selector:
    matchLabels:
      app: infer-classify
  template:
    metadata:
      labels:
        app: infer-classify
    spec:
      {{- include "inference-engine.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: infer-classify
          image: {{ include "inference-engine.image" (dict "name" "infer-classify" "Values" .Values) }}
          imagePullPolicy: {{ .Values.images.pullPolicy }}
          envFrom:
            - configMapRef:
                name: inference-engine-config
          env:
            - name: MODEL_DIR
              value: /app/model
            # Pin PyTorch's intra-op thread pool to the CPU limit so it
            # doesn't thrash on cgroup-throttled cores. Default tracks
            # values.yaml inference.inferClassify.resources.limits.cpu.
            - name: TORCH_NUM_THREADS
              value: {{ .Values.inference.inferClassify.torchThreads | default "2" | quote }}
          readinessProbe:
            exec:
              command: ["test", "-f", "/tmp/ready"]
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 60
          resources:
            {{- toYaml .Values.inference.inferClassify.resources | nindent 12 }}
{{- if .Values.keda.enabled }}
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: infer-classify
  labels:
    app: infer-classify
spec:
  scaleTargetRef:
    name: infer-classify
  minReplicaCount: {{ .Values.inference.inferClassify.minReplicas }}
  maxReplicaCount: {{ .Values.inference.inferClassify.maxReplicas }}
  pollingInterval: {{ .Values.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.keda.cooldownPeriod }}
  triggers:
    - type: nats-jetstream
      metadata:
        natsServerMonitoringEndpoint: "nats-service.{{ .Release.Namespace }}.svc.cluster.local:8222"
        account: "$G"
        stream: "DETECTION_RESULT"
        consumer: "infer-classify"
        lagThreshold: "{{ .Values.keda.lagThreshold }}"
{{- end }}
